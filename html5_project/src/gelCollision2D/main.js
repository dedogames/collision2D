function main()
{

   // Log.getInstance().log(INFO,  "GelRender call GelEngine (pos-mousePos) = "+ dif.toString()+Log.endl());
    Log.getInstance().log(CONSOLE,"\n\n*************************************************\n"+Log.getInstance().endl());
    Log.getInstance().log(CONSOLE,"Base Project cmake using C++17 and multiple projects."+Log.getInstance().endl()+Log.getInstance().endl());
    Log.getInstance().log(CONSOLE,"1 - libGelEngine - Implementation of or Engine"+Log.getInstance().endl());
  //  var eng = new FooEngine(); Log()+Log.getInstance().endl();
    Log.getInstance().log(CONSOLE,"2 - libGelRenderIO - Build a graph and IO library"+Log.getInstance().endl());
  //  var l_render = new FooGelRender(); 
    Log.getInstance().log(CONSOLE,"3 - projGelCollision2D - Join everthing with pratical exemples"+Log.getInstance().endl());
    Log.getInstance().log(CONSOLE,"  - Join projects 1 and 2 with main.cpp" + Log.getInstance().endl()+Log.getInstance().endl());
    Log.getInstance().log(CONSOLE,"\n*************************************************\n"+Log.getInstance().endl());
    Log.getInstance().log(CONSOLE," 'We are not what we know but what we are willing to learn.'" + Log.getInstance().endl());
    Log.getInstance().log(CONSOLE,"  Parker Palmer\n" + Log.getInstance().endl());
    Log.getInstance().log(CONSOLE,"Main executed with " + " arguments" + Log.getInstance().endl() + Log.getInstance().endl()) ;
}

main();